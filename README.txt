#Observer PAttern is adopted as soon as new tweet will happen and if it contains byJus sentiment will be analyzed.

#Sentiment will be immediatel saved to data base to keep record of all sentiments. MongoDB is used as daatabase

#Virtual Dom is used to render html page as it is quite powerful to dynamically update contents and render

#d3.js is used to plot graph


